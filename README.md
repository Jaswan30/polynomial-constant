# polynomial-constant

This project reads polynomial data from a JSON file named `input.json`
(provided at runtime by the evaluator). The values are given in different
number bases and are converted to decimal before computing the constant
term of the polynomial using Lagrange interpolation.

## How to Run
node FindingConstant.js

## Language
JavaScript (Node.js)
